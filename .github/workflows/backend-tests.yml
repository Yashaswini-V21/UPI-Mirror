name: Backend Tests & Coverage

on:
  push:
    branches: [main, develop, feat/**, feature/**]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-asyncio

      - name: Lint with flake8
        continue-on-error: true
        run: |
          pip install flake8 -q
          # Check for critical syntax errors only
          flake8 api/ src/ --count --select=E9,F63,F7,F82 --show-source --statistics || true
          # Additional linting (non-blocking)
          flake8 api/ src/ --count --exit-zero --max-line-length=120 --ignore=E501,W503,E203 || true

      - name: Test with pytest
        env:
          KIRA_AI_API_KEY: ${{ secrets.KIRA_AI_API_KEY || 'test-kira-ci-key-12345678901234567890' }}
          ENVIRONMENT: test
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY || 'test-gemini-key-placeholder' }}
        run: |
          pytest tests/ -v --cov=src --cov-report=xml --cov-report=term-missing --tb=short

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
